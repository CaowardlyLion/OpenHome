# CLI Commands

TUI commands are declared in `internal/commands/registry.go`.

## Built-In Commands

- `/new`: clear in-memory chat and task context.
- `/verbose`: show or hide live execution details in transcript.
- `/exit`: quit OpenHome.

Press `Ctrl+C` during a task to cancel it. Press `Ctrl+C` while idle to exit.
